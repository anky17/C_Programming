#include <stdio.h>
int main()
{
    // WAP to write a table upto 20
    int number;
    int i=1;

    printf("Enter any integer: ");
    scanf("%d", &number);

    for (int i = 1; i <= 10; i++)
    {
    
    }
    printf("%d  * %d =  %d\n", number, i, number * i);
    
    return 0;
}